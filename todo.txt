# Landing Page:
-> Once user hits / route it should be routed to landing page.
-> if user is not logged in, provide a page where they can register/login, with
   dynamic navbar.
-> if user is logged in then should be seeing main page, with dynamic navbar.
